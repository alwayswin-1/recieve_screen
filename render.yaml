services:
  - type: web
    name: recieve-screen
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python receiver_server.py"
    envVars:
      - key: PORT
        value: 10000
      - key: HOST
        value: 0.0.0.0
